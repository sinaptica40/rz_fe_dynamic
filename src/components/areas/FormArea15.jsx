import React from 'react'

export default function FormArea15({children}) {
  return (
    <div className="col-md-6">
    <div className="form-floating">
            {children}
    </div>
</div>
  )
}
